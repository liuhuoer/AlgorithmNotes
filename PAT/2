#include <cstdio>
#include <string>
using std::string;
int main()
{
	//init 1 array;³
	float a[10]={0};
	int n=0,count=0;
	float temp=0;
	//input 2 array;
	for(int i=0;i<=1;++i)
	{
		scanf("%d",&n);
		for(int j=0,k=0;j<n;++j)
		{
			scanf("%d%f",&k,&temp);
			a[k]+=temp;
		}
	}
	//add 
	/*
	for(int i=0;i<10;++i)
	{
		c[i]=a[0][i]+a[1][i];
		if(c[i]!=0)
			++count;
	}
	*/
	for(int i=o;i<10;++i)
		if(c[i]!=0)
			++count;

	//print;
	printf("%d ",count);
	for(int i=9;i>=0;--i)
	{
		if(a[i]!=0)
		{
			printf("%d ",i);
			printf("%0.1f",a[i]);
			if(i!=0)
				printf(" ");
		}
	}
	return 0;
}
